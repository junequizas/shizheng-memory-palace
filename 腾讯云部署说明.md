# 腾讯云静态网站部署说明

本工程已经构建完成。若只想上线，使用压缩包中的 `dist/` 内容即可；若要改代码，再执行 `npm install && npm run build`。

## 方案一：CloudBase 静态网站托管

1. 登录腾讯云控制台，进入云开发 CloudBase，新建或选择一个环境。
2. 打开“静态网站托管”，按控制台提示开通服务。
3. 上传 `dist/` 目录内的全部内容。请上传目录**里面的文件**，不要在访问根目录外再套一层 `dist`。
4. 确认根目录能直接看到 `index.html`、`assets/` 和 `favicon.svg`。
5. 打开控制台给出的默认域名测试。需要自定义域名时，再在域名管理中绑定并按提示配置 DNS。

## 方案二：COS 静态网站

1. 新建一个对象存储 COS 存储桶，地域按主要访问人群选择。
2. 把 `dist/` 内的全部内容上传到存储桶根目录。
3. 在存储桶的“基础配置 / 静态网站”中启用静态网站功能。
4. 索引文档设置为 `index.html`。错误文档也可设置为 `index.html`，便于以后增加前端路由。
5. 使用静态网站访问地址测试；若绑定自定义域名，按控制台提示配置域名与 HTTPS。

## 上线前检查

- 首页能显示知识网络，节点可以拖动，滚轮或双指可以缩放。
- 搜索“新质生产力”后能打开相关节点。
- 卡片第一栏名称为“原文”，显示的是官方来源原句，不是本站概括。
- 卡片右上角可进入卡片全屏；顶部“图谱全屏”只保留图谱与右侧卡片。
- “资料链接”中的官方原文、原文目录、华图和小黑时政入口可以打开。

## 数据保存方式

静态版没有服务器数据库。收藏、个人笔记、复习记录、自建卡片和节点位置保存在当前浏览器的 `localStorage` 中：

- 清理浏览器站点数据会删除这些个人数据。
- 更换浏览器、设备或域名不会自动同步。
- 建议定期使用左侧“导出 Obsidian 笔记”备份。

公共知识库数据仍是结构化数据：官方原文摘录位于 `src/lib/originals.ts`，考点、关键词、易错词、关系和来源位于 `src/lib/catalog.ts`。更新后重新运行 `npm run build`，再覆盖上传新的 `dist/` 内容。
